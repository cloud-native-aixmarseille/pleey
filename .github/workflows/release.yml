name: "• 🚀 Release"

on:
  workflow_dispatch:

concurrency:
  group: release-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: false

permissions: {}

jobs:
  plan-release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      tag: ${{ steps.plan-release.outputs.tag }}
      name: ${{ steps.plan-release.outputs.name }}
    steps:
      - id: plan-release
        uses: hoverkraft-tech/ci-github-publish/actions/release/plan@ba941863a698984b6596e760f4e7727beea81167 # 0.26.0
        with:
          github-token: ${{ github.token }}

  ci:
    needs: [plan-release]
    uses: ./.github/workflows/__shared-ci.yml
    with:
      production-image-tag: ${{ needs.plan-release.outputs.tag }}
    permissions:
      actions: read
      checks: write
      contents: read
      id-token: write
      issues: read
      packages: write
      pull-requests: write
      security-events: write
      statuses: write
    secrets: inherit

  prepare-release-files:
    needs: [plan-release, ci]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release-sha: ${{ steps.create-and-merge-pr.outputs.merged-sha || github.sha }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Update chart image defaults
        uses: hoverkraft-tech/ci-github-container/actions/helm/update-chart-values@8e63086f032651bcd1d44c8f4fcd61b902ebb99f # 0.35.0
        with:
          path: charts/application
          values: >-
            [
              {
                "file":"charts/backend/values.yaml",
                "path":".image.tag",
                "value":"${{ fromJSON(needs.ci.outputs.built-prod-images).backend.tags[0] }}"
              },
              {
                "file":"charts/backend/values.yaml",
                "path":".image.digest",
                "value":"${{ fromJSON(needs.ci.outputs.built-prod-images).backend.digest }}"
              },
              {
                "file":"charts/frontend/values.yaml",
                "path":".image.tag",
                "value":"${{ fromJSON(needs.ci.outputs.built-prod-images).frontend.tags[0] }}"
              },
              {
                "file":"charts/frontend/values.yaml",
                "path":".image.digest",
                "value":"${{ fromJSON(needs.ci.outputs.built-prod-images).frontend.digest }}"
              },
              {
                "file":"Chart.yaml",
                "path":".version",
                "value":"${{ needs.plan-release.outputs.tag }}"},
              {
                "file":"Chart.yaml",
                "path":".appVersion",
                "value":"${{ needs.plan-release.outputs.tag }}"},
              {
                "file":"Chart.yaml",
                "path":"(.dependencies[] | select(.repository == \"file://charts/frontend\").version)",
                "value":"${{ needs.plan-release.outputs.tag }}"
              },
              {
                "file":"Chart.yaml",
                "path":"(.dependencies[] | select(.repository == \"file://charts/backend\").version)",
                "value":"${{ needs.plan-release.outputs.tag }}"
              },
              {
                "file":"charts/backend/Chart.yaml",
                "path":".version",
                "value":"${{ needs.plan-release.outputs.tag }}"
                },
              {
                "file":"charts/backend/Chart.yaml",
                "path":".appVersion",
                "value":"${{ needs.plan-release.outputs.tag }}"
              },
              {
                "file":"charts/frontend/Chart.yaml",
                "path":".version",
                "value":"${{ needs.plan-release.outputs.tag }}"
              },
              {
                "file":"charts/frontend/Chart.yaml",
                "path":".appVersion",
                "value":"${{ needs.plan-release.outputs.tag }}"
              }
            ]

      - name: Set up Helm
        uses: azure/setup-helm@dda3372f752e03dde6b3237bc9431cdc2f7a02a2 # v5.0.0

      - name: Refresh umbrella Chart.lock
        shell: bash
        run: helm dependency update charts/application

      - name: Regenerate Helm chart documentation
        uses: hoverkraft-tech/ci-github-container/actions/helm/generate-docs@8e63086f032651bcd1d44c8f4fcd61b902ebb99f # 0.35.0
        with:
          working-directory: ./charts
          checkout-ref: ""
          github-app-client-id: ${{ vars.CI_BOT_APP_CLIENT_ID }}
          github-app-key: ${{ secrets.CI_BOT_APP_PRIVATE_KEY }}

  draft-release:
    needs: [plan-release, prepare-release-files]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    outputs:
      tag: ${{ steps.create-release.outputs.tag }}
    steps:
      - id: create-release
        uses: hoverkraft-tech/ci-github-publish/actions/release/create@ba941863a698984b6596e760f4e7727beea81167 # 0.26.0
        with:
          tag: ${{ needs.plan-release.outputs.tag }}
          target-sha: ${{ needs.prepare-release-files.outputs.release-sha }}
          publish: "false"
          github-token: ${{ github.token }}

  publish-release-artifacts:
    needs: [plan-release, ci, prepare-release-files, draft-release]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - id: release-chart
        uses: hoverkraft-tech/ci-github-container/actions/helm/release-chart@8e63086f032651bcd1d44c8f4fcd61b902ebb99f # 0.35.0
        with:
          oci-registry: ${{ vars.OCI_REGISTRY }}
          oci-registry-username: ${{ github.repository_owner }}
          oci-registry-password: ${{ github.token }}
          chart: application
          path: charts/application
          helm-repositories: |
            cloudnative-pg https://cloudnative-pg.github.io/charts
            valkey https://valkey.io/valkey-helm/
          tag: ${{ needs.plan-release.outputs.tag }}
          ref: ${{ needs.prepare-release-files.outputs.release-sha }}

  publish-release:
    needs: [draft-release, publish-release-artifacts]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: hoverkraft-tech/ci-github-publish/actions/release/update@ba941863a698984b6596e760f4e7727beea81167 # 0.26.0
        with:
          tag: ${{ needs.draft-release.outputs.tag }}
          publish: "true"
          github-token: ${{ github.token }}
