mutation ResumeDashboardGameSession($sessionId: Int!) {
  resumeGameSession(sessionId: $sessionId) {
    sessionId
    gameId
    pin
    status
    currentStageId
    participantRole
    createdAt
  }
}
