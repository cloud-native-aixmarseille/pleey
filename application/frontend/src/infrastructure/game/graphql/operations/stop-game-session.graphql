mutation StopDashboardGameSession($sessionId: Int!) {
  stopGameSession(sessionId: $sessionId) {
    sessionId
    gameId
    pin
    status
    currentStageId
    participantRole
    createdAt
  }
}
