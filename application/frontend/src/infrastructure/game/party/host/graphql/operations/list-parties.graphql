query ListParties($input: ListPartiesInput) {
  listParties(input: $input) {
    items {
      partyId
      gameId
      pin
      status
      role
      createdAt
    }
    totalCount
    overallCount
    page
    pageSize
    totalPages
  }
}
