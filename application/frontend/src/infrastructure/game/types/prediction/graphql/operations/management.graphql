query PredictionManagement($predictionId: ID!, $input: PaginationInput!) {
  prediction(predictionId: $predictionId) {
    predictionId
    gameId
    title
    description
    createdAt
    promptCount
  }
  predictionPrompts(predictionId: $predictionId, input: $input) {
    ...PredictionManagementItemsPage
  }
}

mutation UpdatePredictionManagement($predictionId: ID!, $input: UpdatePredictionInput!) {
  updatePrediction(predictionId: $predictionId, input: $input) {
    predictionId
  }
}

mutation CreatePredictionManagement($input: CreatePredictionInput!) {
  createPrediction(input: $input) {
    predictionId
  }
}

mutation CreatePredictionFromImportManagement($input: CreatePredictionFromImportInput!) {
  createPredictionFromImport(input: $input) {
    predictionId
    promptCount
  }
}

mutation DeletePredictionManagement($predictionId: ID!) {
  deletePrediction(predictionId: $predictionId)
}

mutation CreatePredictionPromptManagement($input: CreatePredictionPromptInput!) {
  createPredictionPrompt(input: $input) {
    id
    predictionId
    position
    promptText
    timeLimit
    points
    options {
      id
      text
      position
      isCorrect
    }
  }
}

mutation UpdatePredictionPromptManagement($promptId: ID!, $input: UpdatePredictionPromptInput!) {
  updatePredictionPrompt(promptId: $promptId, input: $input) {
    id
    predictionId
    position
    promptText
    timeLimit
    points
    options {
      id
      text
      position
      isCorrect
    }
  }
}

mutation DeletePredictionPromptManagement($promptId: ID!) {
  deletePredictionPrompt(promptId: $promptId)
}

query PredictionManagementItems($predictionId: ID!, $input: PaginationInput!) {
  predictionPrompts(predictionId: $predictionId, input: $input) {
    ...PredictionManagementItemsPage
  }
}

fragment PredictionManagementItemsPage on PredictionPromptListType {
  items {
    id
    predictionId
    position
    promptText
    timeLimit
    points
    options {
      id
      text
      position
      isCorrect
    }
  }
  totalCount
  overallCount
  page
  pageSize
  totalPages
}
