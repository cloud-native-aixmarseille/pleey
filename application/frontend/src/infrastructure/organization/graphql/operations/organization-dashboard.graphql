query WorkspaceOrganizationDashboard($organizationId: Int!) {
  organizationDashboard(organizationId: $organizationId) {
    organization {
      id
      name
      description
    }
    stats {
      totalGames
      totalMembers
      totalProjects
    }
  }
}
