query WorkspaceOrganizationDashboard($organizationId: ID!) {
  organizationDashboard(organizationId: $organizationId) {
    organization {
      id
      name
      description
    }
    stats {
      totalGames
      totalMembers
      totalProjects
    }
  }
}
