query OrganizationMembers($input: OrganizationMembersInput!) {
  organizationMembers(input: $input) {
    items {
      id
      organizationId
      userId
      username
      role
      joinedAt
    }
    totalCount
    overallCount
    page
    pageSize
    totalPages
  }
}
