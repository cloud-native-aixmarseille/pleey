import { uiThemeTokens } from '../../../../../shared/ui/foundation/ui-theme';

export const mobilePlayerSurfaceBackdropStyle = {
  background: uiThemeTokens.color.surface.canvas,
  inset: 0,
  position: 'fixed',
  zIndex: 199,
} as const;
