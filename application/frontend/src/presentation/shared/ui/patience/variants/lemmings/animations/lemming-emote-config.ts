import type { IdleVariantConfig } from '../lemmings-idle-variant-registry';

export const emoteIdleConfig: IdleVariantConfig = {
  variant: 'emote',
  durationMs: 700,
  jitterMs: 700,
  emotes: ['!', '?', '♥', '👋', 'Zzz'],
};
