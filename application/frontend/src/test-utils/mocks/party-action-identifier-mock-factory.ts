import { PartyActionIdentifier } from '../../application/game/party/shared/services/identifiers/party-action-identifier';

export class PartyActionIdentifierMockFactory {
  create() {
    return new PartyActionIdentifier();
  }
}
