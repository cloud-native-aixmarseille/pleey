apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "default.fullname" . }}
  namespace: {{ include "default.namespace" . | quote }}
  labels:
    {{- include "default.labels" . | nindent 4 }}
data:
  VITE_API_URL: {{ .Values.config.apiUrl | quote }}
