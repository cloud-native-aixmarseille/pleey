{{- if .Values.backend.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "pleey.fullname" . }}-backend
  namespace: {{ include "pleey.namespace" . | quote }}
  labels:
    {{- include "pleey.backend.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "pleey.backend.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
---
{{- end }}
{{- if .Values.frontend.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "pleey.fullname" . }}-frontend
  namespace: {{ include "pleey.namespace" . | quote }}
  labels:
    {{- include "pleey.frontend.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "pleey.frontend.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
{{- end }}
